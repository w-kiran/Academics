#include <stdio.h>
#include <string.h>
int main()
{
    int j, i, arr[5] = {2, 3, 4, 5, 1};
    int n;
    printf("%d", n);
    for (i = 0; i < n; i++)
    {
        /* incomplete code */
    }

    return 0;
}