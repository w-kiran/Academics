/*
21. Write a C program that uses the conditional operator to check if a given integer is divisible
by 3 or 5. Print a message to the console indicating the result.*/

#include <stdio.h>
int main()
{
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);
    return 0;
}