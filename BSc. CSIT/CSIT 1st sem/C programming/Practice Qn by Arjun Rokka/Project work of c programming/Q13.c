#include <stdio.h>
int main()
{
    int num;
    float point;
    char ch;
    printf("\nSize of num = %u", sizeof(num));
    printf("\nSize of float numbre = %u", sizeof(point));
    printf("\nSize of character = %u", sizeof(ch));
}